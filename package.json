{
  "name": "nobel.quarterly.systems",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts",
    "test": "bunx tsc --noEmit && bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "yaml": "^2.6.0",
    "marked": "^15.0.0"
  }
}
